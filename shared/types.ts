// Define BookingStep type
export type BookingStep = 'date_selection' | 'checkout_date' | 'guest_selection' | 'booking_summary' | 'payment_selection' | 'confirmation';

// Add other shared types here if needed in the future
